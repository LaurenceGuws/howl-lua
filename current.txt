Current Task

Source:

- `production-grade-scratchpad.md`

Goal:

- Make `howl-lua` production-grade as a small Zig module.
- Keep planning local to this repo instead of cluttering main Howl product trackers.

Repo boundary:

- `howl-lua` is a Zig module, not a C ABI product.
- It still must meet production-grade standards for correctness, ownership clarity, tests, and docs.

Current approach:

1. read the module at a high level first
2. build a focused hardening queue
3. prefer tightening existing surface over adding features
4. keep the public API small and explicit

Strict loop rule for this repo:

- do not pass checkpoint 2 unless the public surface and contract shape are clearly the best small version
- use derive/critique subagent pairs before implementation
- no feature growth while ownership, stack, or lifetime rules are ambiguous

Active next step:

1. lock checkpoint 1 as public-contract lockdown
2. sharpen the ownership, FFI, stack, and borrowed-lifetime contract before general hardening
3. update docs in the same checkpoint so the advertised surface is truthful

Current checkpoint:

- `checkpoint-01-public-contract-lockdown`

Checkpoint 1 must settle:

- `State` ownership semantics
- `getField` field-name FFI contract
- `Reader` / `TableIter` stack-scoped responsibilities
- borrowed string lifetime rules
- README/export truth

Checkpoint 1 chosen shape:

- `State` becomes owning-only
- no public `fromRaw()` in this checkpoint
- safe field lookup keeps Zig-facing `[]const u8` input and moves the C-string hazard out of the public API
- `Reader` becomes a stable table view only
- stack-scoped traversal/iteration responsibility is explicit instead of hidden in generic `Reader` values
- no borrowed `Reader.fieldString()` survives this checkpoint

Checkpoint 2 chosen scope:

- harden `State.loadFile()` failure contract
- harden numeric conversion behavior
- add invalid-input and repeated-failure proofs
- remove `Reader.scalarStringOwned()` instead of trying to preserve its fuzzy semantics

Checkpoint 3 chosen scope:

- harden `Reader.stringOwned()` and `Reader.optionalStringOwned()`
- make owned-string helpers failure-atomic
- prove unchanged-on-missing/nil/non-string behavior
- prove stack neutrality for owned-string helper paths

Checkpoint 4 chosen scope:

- add the smallest supported nested-table replacement
- add the supported ordered string-array accessor
- migrate real host config consumers off removed helpers and raw reach-through
- keep `Reader` as a stable table view and keep stack scope explicit

Checkpoint 5 chosen scope:

- tighten table-only preconditions for `Reader` and `TableIter`
- tighten iterator misuse-edge proofs
- document the remaining borrow window and stack-scope rules clearly

Acceptance target:

- local planning is tracked here, not in the main Howl scratchpads
- checkpoint 1 is explicit and narrow
- no move to general hardening while contract shape still feels second-best
- future passes improve truth, trust, correctness, and completeness rather than chasing feature progress
